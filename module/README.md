# GlassWA root module

Development placeholder for the eventual Magisk/Zygisk packaging layer.
The first working prototype is intentionally implemented as an LSPosed-scoped APK module so we can map WhatsApp's live view hierarchy before introducing native runtime hooks.
